#import os
#from os.path import join

#ALIGNFRAMES = 'alignframes_lmbfgs.csh'

#def _getHome(key, default):
   # """ Get the required home path, if not present..
 #the default value will be used from EM_ROOT.
  #  """
 #   return os.environ.get(key, join(os.environ['EM_ROOT'], default)) #EMRoot path2search4bin /software/em- +zip,unzipped

#ALIGNFRAMES_PATH = join(_getHome('ALIGNFRAMES_HOME', 'alignframes'), 'bin', ALIGNFRAMES)
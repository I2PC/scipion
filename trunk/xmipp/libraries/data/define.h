#ifndef DEFINE_H
#define DEFINE_H

#include "funcs.h"
    #define WRITE_OVERWRITE 0 //forget about the old file and overwrite it
    #define WRITE_APPEND 1    //append and object at the end of a stack, so far can not append stacks
    #define WRITE_REPLACE 2   //replace a particular object by another
#endif
